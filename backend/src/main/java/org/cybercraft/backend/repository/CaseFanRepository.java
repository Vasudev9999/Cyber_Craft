package org.cybercraft.backend.repository;

import org.cybercraft.backend.entity.CaseFan;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CaseFanRepository extends JpaRepository<CaseFan, Long> {
}
